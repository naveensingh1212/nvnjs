//  Immediately Invoked Function Expressions(IIFE)

(function chai(){
    //named IFEE 
    console.log(`sher is connected`);
})();


 ( (name) =>{
    console.log(`sherni is connected ${name}`);
 })('babbr sher')